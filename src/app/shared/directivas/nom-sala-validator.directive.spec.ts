import { NomSalaValidatorDirective } from './nom-sala-validator.directive';

describe('NomSalaValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NomSalaValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
